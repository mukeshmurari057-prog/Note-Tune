# Demo files

The canonical demo notes are in `sample_notes.txt`; they are intentionally short
so the generated output can be inspected in a pull request. Run
`python generate_files.py` after installing the requirements to create matching
`sample_notes.pdf`, `sample_notes.docx`, and `sample_notes.png` fixtures.
`sample_song.txt` shows the expected local-fallback result.
